export type queryParamType = {
    [key: string]: string;
};

export type CellCoordinateType = {
    x: number;
    y: number;
    value: number
}

export type CoordinateType = {
    x: number;
    y: number;
}

export type DetailsModel = {
    image: string;
    number: number;
    state: number;
}
